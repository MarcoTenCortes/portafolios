# Partials

These inline-SVG snippets (`simpl-graph.svg`, `autoencoder.svg`, `qa-terminal.svg`, `lamp.svg`) are pasted by hand into `index.html`; they are not loaded at runtime.
Regenerate or edit them here only when the corresponding visual is redesigned, then re-paste the markup into `index.html`.
