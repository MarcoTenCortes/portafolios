[
  {"name": "lava-1280", "url": "http://localhost:5173/?shot=habilidades", "width": 1280, "height": 800, "settle": 1400, "steps": [{"eval": "window.MTC.ready.then(() => new Promise((r) => setTimeout(r, 150))).then(() => { const q = (s) => document.querySelector(s); const box = (el) => { const r = el.getBoundingClientRect(); return { l: Math.round(r.left), t: Math.round(r.top + scrollY), r: Math.round(r.right), b: Math.round(r.bottom + scrollY), w: Math.round(r.width), h: Math.round(r.height) }; }; const groups = document.querySelectorAll('.skills__group'); const lava = box(q('.skills__lava')), last = box(groups[groups.length - 1]), grid = box(q('.skills__grid')), art = box(q('.skills__lava .lava__art')); q('.skills__lava').scrollIntoView({ behavior: 'instant', block: 'center' }); return JSON.stringify({ lava, art, last, grid, sameRow: lava.t === last.t && lava.b === last.b, insideGrid: lava.l >= grid.l && lava.r <= grid.r && lava.b <= grid.b, fillsRest: lava.r === grid.r, artInside: art.l >= lava.l && art.r <= lava.r && art.t >= lava.t && art.b <= lava.b }); })", "print": true, "wait": 1200}], "out": "tools/out/shots/lava-1280.png"},
  {"name": "lava-1440", "url": "http://localhost:5173/?shot=habilidades", "width": 1440, "height": 900, "settle": 1400, "steps": [{"eval": "window.MTC.ready.then(() => new Promise((r) => setTimeout(r, 150))).then(() => { document.querySelector('.skills__lava').scrollIntoView({ behavior: 'instant', block: 'center' }); const a = document.querySelector('.skills__lava').getBoundingClientRect(), g = document.querySelector('.skills__grid').getBoundingClientRect(); return JSON.stringify({ lava: [Math.round(a.left), Math.round(a.width), Math.round(a.height)], fillsRest: Math.round(a.right) === Math.round(g.right) }); })", "print": true, "wait": 1200}], "out": "tools/out/shots/lava-1440.png"},
  {"name": "lava-768", "url": "http://localhost:5173/?shot=habilidades", "width": 768, "height": 1024, "settle": 1400, "steps": [{"eval": "window.MTC.ready.then(() => new Promise((r) => setTimeout(r, 150))).then(() => { const groups = document.querySelectorAll('.skills__group'); const l = document.querySelector('.skills__lava').getBoundingClientRect(), g = groups[groups.length - 1].getBoundingClientRect(), gr = document.querySelector('.skills__grid').getBoundingClientRect(); document.querySelector('.skills__lava').scrollIntoView({ behavior: 'instant', block: 'center' }); return JSON.stringify({ lava: [Math.round(l.left), Math.round(l.top), Math.round(l.width), Math.round(l.height)], last: [Math.round(g.left), Math.round(g.top), Math.round(g.width), Math.round(g.height)], sameRow: Math.round(l.top) === Math.round(g.top), fillsRest: Math.round(l.right) === Math.round(gr.right) }); })", "print": true, "wait": 1200}], "out": "tools/out/shots/lava-768.png"},
  {"name": "lava-mobile", "url": "http://localhost:5173/?shot=habilidades", "width": 390, "height": 844, "mobile": true, "settle": 1400, "steps": [{"eval": "window.MTC.ready.then(() => new Promise((r) => setTimeout(r, 150))).then(() => { const el = document.querySelector('.skills__lava'); el.scrollIntoView({ behavior: 'instant', block: 'center' }); const r = el.getBoundingClientRect(); return JSON.stringify({ display: getComputedStyle(el).display, lava: [Math.round(r.left), Math.round(r.width), Math.round(r.height)], scrollW: document.documentElement.scrollWidth }); })", "print": true, "wait": 1200}], "out": "tools/out/shots/lava-mobile.png"},
  {"name": "lava-reduced", "url": "http://localhost:5173/?shot=habilidades", "width": 1280, "height": 800, "settle": 1400, "media": [{"name": "prefers-reduced-motion", "value": "reduce"}], "steps": [{"eval": "window.MTC.ready.then(() => new Promise((r) => setTimeout(r, 150))).then(() => { const el = document.querySelector('.skills__lava'); el.scrollIntoView({ behavior: 'instant', block: 'center' }); const anims = el.getAnimations({ subtree: true }).length; const b = el.querySelector('.lava__blob--3'); return JSON.stringify({ runningAnimations: anims, blob3: getComputedStyle(b).translate, artOpacity: getComputedStyle(el.querySelector('.lava__art')).opacity }); })", "print": true, "wait": 300}], "out": "tools/out/shots/lava-reduced.png"},
  {"name": "lava-paused", "url": "http://localhost:5173/?shot=inicio", "width": 1280, "height": 800, "settle": 1600, "steps": [{"eval": "window.MTC.ready.then(() => new Promise((r) => setTimeout(r, 300))).then(() => { const el = document.querySelector('.skills__lava'); const b = el.querySelector('.lava__blob--1'); return JSON.stringify({ offscreen: el.classList.contains('is-offscreen'), blobPlayState: getComputedStyle(b).animationPlayState, glowPlayState: getComputedStyle(el, '::before').animationPlayState, artOpacity: getComputedStyle(el.querySelector('.lava__art')).opacity }); })", "print": true}, {"eval": "document.querySelector('.skills__lava').scrollIntoView({ behavior: 'instant', block: 'center' }); 'scrolled'", "wait": 1400}, {"eval": "(() => { const el = document.querySelector('.skills__lava'); return JSON.stringify({ offscreen: el.classList.contains('is-offscreen'), blobPlayState: getComputedStyle(el.querySelector('.lava__blob--1')).animationPlayState, artOpacity: getComputedStyle(el.querySelector('.lava__art')).opacity }); })()", "print": true}], "out": "tools/out/shots/lava-paused.png"},
  {"name": "lava-detail", "url": "http://localhost:5173/?shot=habilidades", "width": 1280, "height": 800, "scale": 2, "settle": 1400, "steps": [{"eval": "window.MTC.ready.then(() => new Promise((r) => setTimeout(r, 150))).then(() => { document.querySelector('.skills__lava').scrollIntoView({ behavior: 'instant', block: 'center' }); return 'ok'; })", "wait": 1500}], "out": "tools/out/shots/lava-detail.png"}
]
